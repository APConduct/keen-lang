// Test zero-argument function call
// This will help identify if the issue is with zero-argument function calls

// Helper function with no arguments
get_answer() = 42

// Another helper function with arguments (should work)
add(x, y) = x + y

// Function that calls zero-argument function
main() = get_answer()

// Alternative test with arguments
test_with_args() = add(10, 20)
