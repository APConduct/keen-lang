// Minimal test to debug the exact token sequence causing the parsing error

// Test the exact failing pattern in isolation
complex_pipeline = |value| value
    |> |x| x > 50 ? x * 2 : x / 2
    |> |x| x + 10
    |> |x| x < 100 ? x : 100

main() = {
    result = 42
    result
}
