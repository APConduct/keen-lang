// Simple test file to debug pipeline and ternary issues

// Test 1: Simple pipeline with basic functions
simple_test = 5 |> double

// Test 2: Pipeline with lambda
lambda_test = 7 |> |x| x * 2

// Test 3: Simple ternary
ternary_test = 5 > 3 ? 10 : 20

// Test 4: Lambda with ternary (this is what's failing)
lambda_ternary = |x| x > 50 ? x * 2 : x / 2

// Test 5: Pipeline with lambda containing ternary
complex_test = 60 |> |x| x > 50 ? x * 2 : x / 2

// Helper functions
double(x) = x * 2

// Main function to test
main() = {
    result1 = simple_test
    result2 = lambda_test
    result3 = ternary_test
    result1 + result2 + result3
}
