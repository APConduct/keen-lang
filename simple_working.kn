// Simple Working Keen Test
// Testing only the most basic features that should work

// Basic arithmetic functions
add(x, y) = x + y
subtract(x, y) = x - y
multiply(x, y) = x * y
divide(x, y) = x / y

// Simple variables
a = 10
b = 5
name = "Alice"

// Simple function calls using literals instead of global variables
sum = add(10, 5)
product = multiply(10, 5)

// Main function using literals
main() = add(15, 50)

// Test constants
pi = 3
result = multiply(pi, 2)
