// Test single line vs multiline complex pipeline

// Single line version (this should work)
single_line = |value| value |> |x| x > 50 ? x * 2 : x / 2 |> |x| x + 10

// Multiline version (this might be failing)
multiline = |value| value
    |> |x| x > 50 ? x * 2 : x / 2
    |> |x| x + 10

// Simple test
simple = 5 |> |x| x * 2

main() = {
    result = simple
    result
}
