// Test file with only the features we've confirmed are working

// Simple variables
x = 42
y = 100
name = "Alice"

// Simple function with block body (this was working)
main() {
    result = x + y
    result
}

// Simple function with expression body
add(a, b) = a + b

// Variable with constructor expression (this should now work)
user_id: UserId = UserId(id: 12345)

// Variable with string interpolation (this should work)
greeting = "Hello, {name}!"
