// Simplest possible pipeline test
// Just tests if the pipeline operator |> works at all

// Simple helper function
double(x) = x * 2

// Very simple pipeline test
main() = {
    result = 5 |> double
    result
}
