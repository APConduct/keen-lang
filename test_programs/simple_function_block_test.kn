// Simple Function Block Test
// Test the most basic function with block body

// Simple function with expression body
add(a, b) = a + b

// Simple function with block body
calculate(x, y) {
    sum = x + y
    sum
}

// Global variables
a = 5
b = 10

// Variable assignment with function call
result = add(a, b)

// Main function
main() = result
