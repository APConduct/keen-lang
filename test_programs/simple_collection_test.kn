// Simple Collection Test for Keen Language
// Tests basic list and map literals with chumsky parser

// Global variables
x = 10
y = 20

// Simple functions
add(a, b) = a + b

// Test empty collections
empty_list = []
empty_map = {}

// Test simple list literal
numbers = [1, 2, 3]

// Test simple map literal
simple_map = {"name": "test", "value": 42}

// Test list with variables
var_list = [x, y]

// Test map with variables
var_map = {"x": x, "y": y}

// Main function
main() {
    // For now, just return a simple value to test parsing
    add(x, y)
}
