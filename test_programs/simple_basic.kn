// Very Simple Test Program for Keen Language
// Tests only the most basic features without complex expressions

// Simple variables
x = 10
y = 20

// Simple function with expression body
add(a, b) = a + b

// Simple function with block body
main() {
    result = add(x, y)
    result
}
