// Test one-line version of complex pipeline to isolate multiline parsing issues

// Test the exact failing pattern but on one line
complex_pipeline_oneline = |value| value |> |x| x > 50 ? x * 2 : x / 2 |> |x| x + 10 |> |x| x < 100 ? x : 100

// Test multiline version (this is the one that fails)
complex_pipeline_multiline = |value| value
    |> |x| x > 50 ? x * 2 : x / 2
    |> |x| x + 10
    |> |x| x < 100 ? x : 100

// Simpler test cases
simple_pipeline = 5 |> |x| x * 2
lambda_test = |x| x > 5 ? x * 2 : x / 2

main() = {
    result = simple_pipeline
    result
}
