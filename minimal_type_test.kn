// Minimal type annotation test to isolate parsing issues

// Simple function without types (should work)
add_simple(x, y) = x + y

// Function with one parameter type
add_one_type(x: Int, y) = x + y

// Function with return type only
get_number(): Int = 42

main() = {
    result = add_simple(5, 3)
    result
}
