// Test with both functions having block expressions
// This should work if the issue is that only one function has blocks

// First block function
test() = {
    42
}

// Second block function (not just expression)
main() = {
    result = test()
    result
}
