// Simple when expression test to isolate the parsing issue

x = 85

test_when(score) {
    when score {
        90 -> "A"
        80 -> "B"
        70 -> "C"
        _ -> "F"
    }
}

main() {
    result = test_when(x)
    42
}
