// Exact reproduction of the failing when expression

classify_number(n: Int): Int = {
    when n {
        < 0 -> -1
        == 0 -> 0
        > 0 -> 1
        _ -> 999
    }
}

main(): Int = {
    result = classify_number(5)
    result
}