// Simple test file to verify parser routing fixes
// This should use chumsky parser (no complex features)

// Simple variables
x = 10
y = 20
name = "Alice"

// Simple functions
add(a, b) = a + b
multiply(x, y) = x * y

// Function with simple block
calculate(a, b) {
    sum = a + b
    product = a * b
    sum + product
}

// Main function
main() {
    result = add(x, y)
    calc_result = calculate(5, 7)
    result + calc_result
}
